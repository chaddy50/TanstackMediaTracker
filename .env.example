
# This file contains placeholder values only and is safe to commit.
# Copy it to .env and fill in the real values before running the app.

# ── Database ──────────────────────────────────────────────────────────────────
# PostgreSQL connection string
DATABASE_URL="postgresql://media_tracker:media_tracker@localhost:5432/media_tracker"

# ── Better Auth ───────────────────────────────────────────────────────────────
# The publicly reachable URL of the app (used for OAuth callbacks and reset links)
BETTER_AUTH_URL=http://localhost:8484
# Random secret used to sign sessions — generate with: openssl rand -hex 32
BETTER_AUTH_SECRET=

# ── TMDB (movies & TV) ────────────────────────────────────────────────────────
# Free API key from https://developer.themoviedb.org — create an account, then
# go to Settings → API and request a key (approved instantly)
TMDB_API_KEY=

# ── IGDB (video games) ────────────────────────────────────────────────────────
# Requires a Twitch developer app at https://dev.twitch.tv/console
# Create an app → copy the Client ID and generate a Client Secret
IGDB_CLIENT_ID=
IGDB_CLIENT_SECRET=

# ── Hardcover (books) ─────────────────────────────────────────────────────────
# Personal API token from https://hardcover.app/account/api
# Must include the "Bearer " prefix (e.g. HARDCOVER_API_KEY="Bearer abc123...")
HARDCOVER_API_KEY=

# ── Gmail SMTP (password reset emails) ───────────────────────────────────────
# Your Gmail address
SMTP_USER=
# App Password (not your regular password) — requires 2FA on the account
# Generate one at https://myaccount.google.com/apppasswords
SMTP_APP_PASSWORD=

# ── Sentry (error monitoring) ─────────────────────────────────────────────────
# All values come from your project at https://sentry.io
# DSN: Settings → Projects → <project> → Client Keys
VITE_SENTRY_DSN=
# Your Sentry organisation slug (visible in the URL: sentry.io/organizations/<slug>)
VITE_SENTRY_ORG=
# Your Sentry project slug (visible in Settings → Projects)
VITE_SENTRY_PROJECT=
# Auth token for source map uploads: https://sentry.io/settings/auth-tokens
SENTRY_AUTH_TOKEN=
